diff --git a/src/locales/default/home.ts b/src/locales/default/home.ts
index c05310dce..fef414318 100644
--- a/src/locales/default/home.ts
+++ b/src/locales/default/home.ts
@@ -61,4 +61,7 @@ export default {
   'starter.newLabel': 'New',
   'starter.videoGeneration': 'Seedance 2.0',
   'starter.write': 'Write',
+  'starter.Qwen3.6-35B': 'Qwen3.6-35B-A3B-FP8',
+  'starter.Qwen3.6-35BAlready': 'Already using Qwen3.6-35B',
+  'starter.Qwen3.6-35BSwitched': 'Switched to Qwen3.6-35B',
 };
diff --git a/src/routes/(main)/home/features/InputArea/StarterList.tsx b/src/routes/(main)/home/features/InputArea/StarterList.tsx
index 2019ae690..9749507a2 100644
--- a/src/routes/(main)/home/features/InputArea/StarterList.tsx
+++ b/src/routes/(main)/home/features/InputArea/StarterList.tsx
@@ -1,4 +1,4 @@
-import { DeepSeek, Jimeng, OpenAI } from '@lobehub/icons';
+import { Qwen } from '@lobehub/icons';
 import { type ButtonProps } from '@lobehub/ui';
 import { Button, Center, Tag, Tooltip } from '@lobehub/ui';
 import { App } from 'antd';
@@ -12,9 +12,9 @@ import { useAgentStore } from '@/store/agent';
 import { agentByIdSelectors } from '@/store/agent/selectors';
 
 import { useResolvedHomeAgentId } from '../AgentSelect/useResolvedHomeAgentId';
-import { DEEPSEEK_V4_PRO_MODEL, DEEPSEEK_V4_PRO_PROVIDER } from './starterModels';
+import { QWEN3_6_35B_MODEL, QWEN3_6_35B_PROVIDER } from './starterModels';
 
-type StarterKey = 'image' | 'video' | 'deepseek-v4-pro';
+type StarterKey = 'image' | 'video' | 'Qwen3.6-35B';
 
 const styles = createStaticStyles(({ css, cssVar }) => ({
   button: css`
@@ -37,7 +37,7 @@ const styles = createStaticStyles(({ css, cssVar }) => ({
 type StarterTitleKey =
   | 'starter.imageGeneration'
   | 'starter.videoGeneration'
-  | 'starter.deepseekV4Pro';
+  | 'starter.Qwen3.6-35B';
 
 interface StarterItem {
   disabled?: boolean;
@@ -57,19 +57,9 @@ const StarterList = memo(() => {
   const items: StarterItem[] = useMemo(
     () => [
       {
-        icon: DeepSeek.Avatar,
-        key: 'deepseek-v4-pro',
-        titleKey: 'starter.deepseekV4Pro',
-      },
-      {
-        icon: OpenAI.Avatar,
-        key: 'image',
-        titleKey: 'starter.imageGeneration',
-      },
-      {
-        icon: Jimeng.Avatar,
-        key: 'video',
-        titleKey: 'starter.videoGeneration',
+        icon: Qwen.Avatar,
+        key: 'Qwen3.6-35B',
+        titleKey: 'starter.Qwen3.6-35B',
       },
     ],
     [],
@@ -87,7 +77,7 @@ const StarterList = memo(() => {
         return;
       }
 
-      if (key === 'deepseek-v4-pro') {
+      if (key === 'Qwen3.6-35B') {
         if (!activeAgentId || switchingKey) return;
         setSwitchingKey(key);
         try {
@@ -103,19 +93,16 @@ const StarterList = memo(() => {
           const currentModel = agentByIdSelectors.getAgentModelById(activeAgentId)(agentState);
           const currentProvider =
             agentByIdSelectors.getAgentModelProviderById(activeAgentId)(agentState);
-          if (
-            currentModel === DEEPSEEK_V4_PRO_MODEL &&
-            currentProvider === DEEPSEEK_V4_PRO_PROVIDER
-          ) {
-            message.info(t('starter.deepseekV4ProAlready'));
+          if (currentModel === QWEN3_6_35B_MODEL && currentProvider === QWEN3_6_35B_PROVIDER) {
+            message.info(t('starter.Qwen3.6-35BAlready'));
             return;
           }
 
           await updateAgentConfigById(activeAgentId, {
-            model: DEEPSEEK_V4_PRO_MODEL,
-            provider: DEEPSEEK_V4_PRO_PROVIDER,
+            model: QWEN3_6_35B_MODEL,
+            provider: QWEN3_6_35B_PROVIDER,
           });
-          message.success(t('starter.deepseekV4ProSwitched'));
+          message.success(t('starter.Qwen3.6-35BSwitched'));
         } finally {
           setSwitchingKey(null);
         }
diff --git a/src/routes/(main)/home/features/InputArea/starterModels.ts b/src/routes/(main)/home/features/InputArea/starterModels.ts
index 4d823defc..704508b9c 100644
--- a/src/routes/(main)/home/features/InputArea/starterModels.ts
+++ b/src/routes/(main)/home/features/InputArea/starterModels.ts
@@ -2,3 +2,6 @@ import { ENABLE_BUSINESS_FEATURES } from '@lobechat/business-const';
 
 export const DEEPSEEK_V4_PRO_MODEL = 'deepseek-v4-pro';
 export const DEEPSEEK_V4_PRO_PROVIDER = ENABLE_BUSINESS_FEATURES ? 'lobehub' : 'deepseek';
+
+export const QWEN3_6_35B_MODEL = 'Qwen3.6-35B-A3B-FP8';
+export const QWEN3_6_35B_PROVIDER = ENABLE_BUSINESS_FEATURES ? 'lobehub' : 'vllm';
